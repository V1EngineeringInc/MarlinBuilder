#!/bin/bash

export V1_ROOT=${V1_ROOT:-$(cd "$(dirname "$0")/../.."; pwd)}
export SCRIPTDIR="${SCRIPTDIR:-${V1_ROOT}/src}"
export COREDIR="${COREDIR:-${SCRIPTDIR}/core}"
export CFGDIR="${CFGDIR:-$SCRIPTDIR/configs}"
export MARLINDIR="${MARLINDIR:-${V1_ROOT}/Marlin}"

